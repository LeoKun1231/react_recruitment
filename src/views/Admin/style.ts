import styled from 'styled-components'

export const AdminWrapper = styled.div`
  height: 100vh;
  min-width: 1200px;
  .trigger {
    padding: 0 24px;
    font-size: 18px;
    line-height: 48px;
    cursor: pointer;
    transition: color 0.3s;
  }

  .trigger:hover {
    color: #1890ff;
  }
`
